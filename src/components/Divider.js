import React from "react";

const Divider = ({ className }) => {
  return (
    <div
      className={`border-t border-[#EEEEEE] mt-[14px] mb-16px ${className}`}
    ></div>
  );
};

export default Divider;
